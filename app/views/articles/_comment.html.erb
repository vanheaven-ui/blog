<div>
  <h4>Comment By: <%= comment.commenter %></h4>
  <p><%= comment.body %></p>
  <%= link_to 'Delete Comment', article_comment_path(comment),
                                method: :delete,
                                data: {confirm: 'Are you sure?'} %>
</div>